# 🀄 雀神争霸 AI 策略算法详解

## 一、核心策略：向听数 + 启发式评分

这个 AI 使用的是**经典的麻将 AI 策略**：以**向听数**（Shanten，距离听牌还差几张）为主导，结合多种**启发式规则**来评估每张牌的价值。

---

## 二、向听数算法（Shanten Calculation）

### 2.1 什么是向听数？

**向听数 = 距离听牌（可以胡牌）还需要换几张牌**

- `-1` = 已经胡牌
- `0` = 听牌（还差一张就能胡）
- `1` = 一向听（还需要换 1 张）
- `2` = 二向听（还需要换 2 张）
- ...以此类推

### 2.2 两种向听数计算

#### A. 标准型向听数 (`standard_shanten`)

适用于：**面子和胡** = 4 组面子（顺子/刻子）+ 1 对雀头

**算法核心**（`shanten.py:16-33`）：
```python
向听数 = 8 - (面子数×2) - 搭子数 - 雀头
```

**递归搜索**（`_best_meld_taatsu`）：
1. 从左到右扫描每张牌
2. 尝试 5 种拆分方式：
   - 孤张（不组合）
   - 刻子（3 张相同）
   - 顺子（连续 3 张）
   - 对子（2 张相同，作为搭子）
   - 两面/边张搭子（2 张连续）
   - 嵌张搭子（2 张间隔 1）
3. 递归计算剩余牌的最优组合
4. 使用 `lru_cache` 缓存（最多 20 万条）避免重复计算

**例子**：
```
手牌: 1m 2m 3m 5m 6m 7m 8m 9m 1p 1p 2p 3p 4p
分析:
- 面子: [1m 2m 3m], [5m 6m 7m], [2p 3p 4p] = 3 个
- 搭子: [8m 9m], [1p 1p] = 2 个
- 雀头: 无
- 向听数 = 8 - (3×2) - 2 - 0 = 0（听牌！）
```

#### B. 七对子向听数 (`seven_pairs_shanten`)

适用于：**七对子胡** = 7 个对子

**算法**（`shanten.py:8-13`）：
```python
对子单位数 = sum(每张牌数量 // 2)
if 对子单位数 >= 7:
    return -1  # 已胡牌
else:
    return 6 - 对子单位数
```

**例子**：
```
手牌: 1m 1m 3m 3m 5m 5m 7m 7m 9m 9m 1p 1p 3p
对子数: 6 个
向听数 = 6 - 6 = 0（听牌，还差一张变成对子）
```

#### C. 最优向听数 (`best_shanten`)

```python
return min(标准型向听, 七对子向听)
```

AI 会自动选择更接近听牌的路线。

---

## 三、打牌评分系统（`_score_discard`）

对手牌中的每张牌，计算"打掉它的价值"，**分数越高 = 越该打掉**。

### 3.1 核心评分项（按权重排序）

#### ① 向听数变化（权重最高，-120 分/向听）

```python
score += -best_shanten(打掉后的牌) * 120
score += -standard_shanten(打掉后的牌) * 18
score += -seven_pairs_shanten(打掉后的牌) * 16
```

- 打掉后向听数**不变** → 0 分
- 打掉后向听数**+1**（变差）→ -120 分（强烈不建议）
- 打掉后向听数**-1**（变好）→ +120 分（优先打）

**这是 AI 的核心决策依据：优先打不影响或改善向听数的牌。**

#### ② 孤张加分（+18 ~ +36 分）

```python
if count == 1:  # 单张
    score += 18
    if neighbor_count == 0:  # 周围没有连接牌
        score += 18  # 孤张总计 +36
    elif neighbor_count == 1:  # 只有 1 张邻居
        score += 7   # 总计 +25
```

**逻辑**：孤张（特别是完全孤立的）进张面窄，优先打掉。

#### ③ 对子/刻子惩罚（-25 ~ -42 分）

```python
elif count == 2:
    score -= 30  # 对子很有价值
elif count == 3:
    score -= 42  # 刻子非常有价值
elif count == 4:
    score -= 25  # 四张牌（特殊，可能杠或七对）
```

**逻辑**：对子和刻子是组成面子的基础，保留价值高。

#### ④ 幺九牌加分（+8 ~ +16 分）

```python
if rank in (1, 9):  # 1 或 9
    score += 8
    if neighbor_count == 0:  # 孤张幺九
        score += 8  # 总计 +16
```

**逻辑**：边张（1 和 9）进张面窄（只能 123 或 789），优先打掉。

#### ⑤ 七对子路线调整（-35 分）

```python
pair_units = sum(value // 2 for value in counts.values())
if not game_state.self_melds and pair_units >= 5 and count >= 2:
    score -= 35  # 保留对子
```

**逻辑**：如果已经有 5+ 个对子且未碰牌，往七对子路线走。

#### ⑥ 将对路线调整（-22 分）

```python
jiang_count = sum(...)  # 统计 2、5、8（将牌）
if jiang_count >= 9 and rank in (2, 5, 8):
    score -= 22  # 保留将牌
```

**逻辑**：《燕云十六声》特有番型"将对"，2、5、8 是宝牌。

#### ⑦ 清一色路线调整（+24 或 -14 分）

```python
if dominant_count >= 9:  # 某花色 ≥9 张
    if suit != dominant_suit:
        score += 24  # 打掉其他花色
    else:
        score -= 14  # 保留主花色
```

**逻辑**：如果某花色占绝对优势，往清一色方向走。

#### ⑧ 可见牌数量（+6 ~ +12 分）

```python
visible_count = visible[tile]  # 对手打出的 + 副露的
if visible_count >= 3:
    score += 12  # 已经看见 3 张，第 4 张价值降低
```

**逻辑**：对手已经打出很多的牌，自己留着进张希望低。

#### ⑨ 等待牌死张检测（+4 ~ +10 分/死张）

```python
wait_tiles = _useful_neighbor_tiles(tile)  # 周围 ±1、±2 的牌
dead_waits = sum(1 for wait in wait_tiles if visible[wait] >= 4)  # 4 张全见
thin_waits = sum(1 for wait in wait_tiles if visible[wait] >= 3)  # 见 3 张

if dead_waits:
    score += dead_waits * 10  # 等待牌全死，打掉
if thin_waits:
    score += thin_waits * 4   # 等待牌见 3 张，略加分
```

**逻辑**：如果留着这张牌等的牌已经全部可见，留着没用。

---

## 四、特殊规则处理

### 4.1 定缺强制打（+10000 分）

```python
missing_tiles = [tile for tile in discard_scores if matches_missing_suit(tile, missing_suit)]
if missing_tiles:
    candidate_scores = {tile: discard_scores[tile] + 10000 for tile in missing_tiles}
```

**逻辑**：四川麻将规则，定缺后必须立即打出缺门牌，加 1 万分保证最优先。

### 4.2 定缺选择算法（`recommend_missing_suit`）

如果游戏还未开始定缺，AI 会建议：

```python
def _missing_suit_score(tiles: list[str]) -> float:
    score = len(tiles) * 100.0  # 基础：牌越多分越高（不要选）
    score += sum((count - 1) * 35 for count in counts.values() if count >= 2)  # 对子加分
    score += 将牌加分 (2, 5, 8)
    score += 连张加分 (有顺子潜力)
    return score
```

**选择逻辑**：
1. 计算每个花色的"价值分"
2. 选择**分数最低**的花色定缺
3. 保留对子多、连张多、将牌多的花色

**例子**：
```
手牌: 1m 2m 3m 4m 5m 6m (6 张万)
      1p 3p 5p (3 张筒)
      2s 5s 8s 9s (4 张条)

万: 6×100 + 连张加分 = 高分（不选）
筒: 3×100 + 将牌加分 = 中等分
条: 4×100 + 将牌加分 = 较高分

建议定缺: 筒（分最低）
```

---

## 五、路线识别（`analyze_routes`）

AI 会分析手牌可能的番型路线，影响打牌决策：

```python
routes = []

# 1. 先打缺门（最高优先级）
if 手里还有缺门牌:
    routes.append("先打缺门")

# 2. 七对子系列
if seven_pairs_shanten(tiles) <= 2:
    if quad_count >= 1:
        routes.append("龙七对路线")  # 四川麻将特有，4 张相同算 2 对
    else:
        routes.append("七对路线")

# 3. 将对系列
if jiang_count >= 9:  # 2、5、8 合计 9+ 张
    if pair_units >= 5:
        routes.append("将七对路线")  # 七对 + 将牌
    else:
        routes.append("将对路线")

# 4. 清一色
if dominant_count >= 9:  # 某花色 9+ 张
    routes.append(f"清一色路线（{suit_label(dominant_suit)}）")

# 5. 碰碰胡
if 刻子或准刻子 >= 3:
    routes.append("碰碰胡路线")

# 6. 向听状态
if min(standard, seven) <= 1:
    routes.append("接近听牌")
elif standard <= 3:
    routes.append("平胡牌效率")
```

---

## 六、最终决策流程

```
1. 检查手牌是否有效（无 X 牌、13/14 张）
   ↓
2. 计算所有候选打牌的分数
   for 每张不重复的牌:
       分数 = 向听数变化×120 + 孤张加分 + 对子惩罚 + ... (10+ 项)
   ↓
3. 特殊规则覆盖
   if 有缺门牌:
       缺门牌分数 += 10000（强制最高）
   ↓
4. 排序选出 Top 3
   推荐打 = 分数最高的牌
   备选 = 分数第 2、3 高的牌
   ↓
5. 生成原因文案
   根据路线和牌的特征（孤张、幺九、清一色等）生成中文解释
```

---

## 七、策略特点总结

### ✅ 优势

1. **向听数为核心** - 符合竞技麻将的基本原则（快速听牌）
2. **启发式规则丰富** - 覆盖孤张、对子、幺九、可见牌等多维度
3. **特殊番型支持** - 七对、将对、清一色、碰碰胡
4. **缺门规则硬编码** - 四川麻将特有规则，强制优先级
5. **性能优化良好** - lru_cache 缓存向听数计算，避免重复
6. **可解释性强** - 每个建议都有中文原因说明

### ⚠️ 局限性

1. **无防守策略** - 不考虑对手听牌、危险牌
2. **无胡牌价值评估** - 不区分"快速听牌"和"听大牌"
3. **无概率计算** - 不计算进张枚数、胡牌概率
4. **固定权重** - 启发式规则权重是硬编码的，未经机器学习优化
5. **无读牌能力** - 不根据对手打牌推测手牌
6. **无牌效率深度计算** - 不计算"有效牌"（改良张）的精确枚数

### 🎯 适用场景

- **初学者教学** - 帮助理解基本的打牌思路
- **练习模式辅助** - 提供参考建议，不追求绝对最优
- **快速决策** - 0.8 秒内给出建议，实时性好

### 🚀 进阶方向

如果要提升到竞技级别，需要：
1. 增加**防守模块**（危险牌检测）
2. 引入**蒙特卡洛模拟**（计算胡牌期望值）
3. 使用**深度学习**（如天凤 AI 的神经网络）
4. 增加**牌谱学习**（从高手对局中学习）

---

## 八、实战例子

```
手牌: 1m 2m 3m 4m 5m 6m 7m 8m 1p 1p 3p 5p 7p 9p (14 张，刚摸牌)
定缺: 条

AI 分析过程:

1. 向听数计算
   - 打 1p: 剩余向听数 = 1（接近听牌）
   - 打 3p: 剩余向听数 = 2
   - 打 5p: 剩余向听数 = 2
   - 打 7p: 剩余向听数 = 2
   - 打 9p: 剩余向听数 = 1

2. 详细评分（以 9p 为例）
   - 向听数: -1 × 120 = -120（打掉后一向听，优秀）
   - 孤张: +18
   - 幺九: +8
   - 孤张幺九: +8
   - 总分: -120 + 18 + 8 + 8 = -86

3. 对比其他牌
   - 1p: -86（对子，-30 惩罚，总分较低）
   - 9p: -86（孤张幺九，加分多）
   
4. 最终建议
   推荐打: 9p
   原因: "推荐打九筒。它是边张单牌，进张面较窄，目前保留价值偏低。"
```

---

## 九、与其他麻将 AI 的对比

| 特性 | 雀神争霸 AI | 天凤凤凰 | Suphx（微软） |
|------|------------|---------|---------------|
| **核心算法** | 向听数+启发式 | 深度强化学习 | 深度强化学习 |
| **响应速度** | 0.8秒 | 实时 | 实时 |
| **防守能力** | ❌ 无 | ✅ 强 | ✅ 极强 |
| **读牌能力** | ❌ 无 | ✅ 有 | ✅ 强 |
| **训练数据** | 规则编码 | 百万局牌谱 | 千万局对战 |
| **水平** | 初中级 | 高段位 | 天凤十段+ |
| **可解释性** | ✅ 每步都有原因 | ⚠️ 黑盒 | ⚠️ 黑盒 |
| **资源需求** | 低（CPU） | 中（GPU可选） | 高（GPU） |

---

**总结**：这是一个**经典的向听数 + 启发式评分**系统，适合教学和辅助练习，性能优秀（<1秒响应），可解释性强，但缺乏防守和深度策略，不是顶尖竞技级别的 AI。
